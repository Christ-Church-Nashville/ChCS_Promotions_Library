//try{
//  config.staffData = {
//    //
//  };
//  if(config.debug) config.errorNotificationEmail.push('bob+ccn-library-staffData@rupholdt.com');///only set if cnofig.debug is enabled
//  if(config.debug) config.errorNotificationEmail = ['bob+ccn-library-staffData@rupholdt.com'];///during development, don't pester chad = turn off at golive
//}
//catch(e){ err('Unable to set config.fileTransfer') }
